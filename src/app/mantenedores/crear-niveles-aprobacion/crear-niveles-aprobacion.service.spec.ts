/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { CrearNivelesAprobacionService } from './crear-niveles-aprobacion.service';

describe('Service: CrearNivelesAprobacion', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CrearNivelesAprobacionService]
    });
  });

  it('should ...', inject([CrearNivelesAprobacionService], (service: CrearNivelesAprobacionService) => {
    expect(service).toBeTruthy();
  }));
});
